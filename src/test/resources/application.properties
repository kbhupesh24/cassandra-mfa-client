# ============================================
# Plain JDBC Test Configuration (PlainJdbcMfaTest)
# ============================================
azure.tenant-id=${AZURE_TENANT_ID}
azure.client-id=${AZURE_CLIENT_ID}
azure.client-secret=${AZURE_CLIENT_SECRET}
azure.scope=api://836282d7-675d-40f0-997b-b3522d97a7ab/.default

cassandra.host=127.0.0.1
cassandra.port=9042
cassandra.dc=datacenter1
cassandra.ssl-enabled=false

# SSL Configuration (only needed if ssl-enabled=true)
#cassandra.truststore=/path/to/truststore.jks
#cassandra.truststore-password=changeit

# ============================================
# Spring Boot Test Configuration (SpringCassandraMfaTest)
# ============================================
cassandra-mfa.tenant-id=${AZURE_TENANT_ID}
cassandra-mfa.client-id=${AZURE_CLIENT_ID}
cassandra-mfa.client-secret=${AZURE_CLIENT_SECRET}
cassandra-mfa.scope=api://836282d7-675d-40f0-997b-b3522d97a7ab/.default
cassandra-mfa.host=127.0.0.1
cassandra-mfa.port=9042
cassandra-mfa.local-dc=datacenter1
cassandra-mfa.ssl-enabled=false
